import 'package:flutter/material.dart';


// Vertical Space 5 
class Space05v extends StatelessWidget {
  const Space05v({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 05,);
  }
}

// Horizontal Space 5
class Space05h extends StatelessWidget {
  const Space05h({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 05,);
  }
}


// Vertical Space 10 
class Space10v extends StatelessWidget {
  const Space10v({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 10,);
  }
}

// Horizontal Space 10
class Space10h extends StatelessWidget {
  const Space10h({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 10,);
  }
}

// Vertical Space 15
class Space15v extends StatelessWidget {
  const Space15v({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 15,);
  }
}

// Horizontal Space 15
class Space15h extends StatelessWidget {
  const Space15h({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 10,);
  }
}

// Vertical Space 20
class Space20v extends StatelessWidget {
  const Space20v({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 20,);
  }
}


// Horizontal Space 20
class Space20h extends StatelessWidget {
  const Space20h({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 10,);
  }
}

// Vertical Space 25 
class Space25v extends StatelessWidget {
  const Space25v({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 25,);
  }
}

// Horizontal Space 25
class Space25h extends StatelessWidget {
  const Space25h({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 25,);
  }
}

// Vertical Space 30 
class Space30v extends StatelessWidget {
  const Space30v({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 30,);
  }
}

// Horizontal Space 30
class Space30h extends StatelessWidget {
  const Space30h({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 30,);
  }
}

class BottomSpace extends StatelessWidget {
  const BottomSpace({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 90,);
  }
}